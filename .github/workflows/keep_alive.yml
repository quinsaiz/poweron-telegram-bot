name: Keep Koyeb Alive

on:
  schedule:
    - cron: '*/40 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Koyeb App
        run: |
          curl -s -o /dev/null -w "%{http_code}" parliamentary-lesley-poweron-telegram-bot-a491d359.koyeb.app/
          echo "Ping sent to Koyeb"